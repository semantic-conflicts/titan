
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile assembly:single

before_deploy:
    - mkdir MiningBuild
     - find . -name '*titan-core*with-dependencies.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/titan/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: without-fetchjar-43eef3b1e
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            
